# Example of training MLP using different optimization packages

This repository contains a code for a multilayer perceptron model
along with loss functions to train such a model to satisfy
given data (x, y).
